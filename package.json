{
  "name": "zearch-radar-crawler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Product Radar crawler for Zearch Engine — finds high-demand, sourceable products and POSTs them to the sourcing-portal /ingest/radar endpoint. Runs as a GitHub Actions cron (Workers can't do long crawls).",
  "scripts": {
    "crawl": "tsx src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
